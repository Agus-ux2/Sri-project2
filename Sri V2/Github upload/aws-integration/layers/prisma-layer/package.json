{
    "name": "prisma-layer",
    "version": "1.0.0",
    "description": "Prisma Client Layer for AWS Lambda",
    "dependencies": {
        "@prisma/client": "^5.8.0"
    },
    "devDependencies": {
        "prisma": "^5.8.0"
    },
    "scripts": {
        "generate": "prisma generate",
        "postinstall": "prisma generate"
    }
}